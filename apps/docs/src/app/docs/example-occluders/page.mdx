import { BlankScene } from "../../(components)/BlankScene.tsx";
import { GLTFModel } from "../../(components)/GLTFModel.tsx";
import { OBJModel } from "../../(components)/OBJModel.tsx";

# Example Occluders

## Glasses

For glasses two rectangle, one for each ear is good enough.
[Download Ear Occluder](/ear_occluder.glb)

<BlankScene key="glasses">
  <group scale={[0.2, 0.2, 0.2]}>
    <GLTFModel modelPath="/canonical_face_model.glb" />
    <GLTFModel modelPath="/ear_occluder.glb" />
    <GLTFModel modelPath="/glasses.glb" />
  </group>
</BlankScene>

## Cap

For this cap we've used cylinder with different top and bottom radius.
The top end of the cylinder covers the inside of the cap.
The bottom end of the cylinder covers parts behind forehead.

[Download Occluder](/baseball_cap_ny_occluder.glb)

<BlankScene key="cap">
  <group scale={[0.2, 0.2, 0.2]}>
    <GLTFModel modelPath="/canonical_face_model.glb" />
    <GLTFModel modelPath="/baseball_cap_ny_occluder.glb" />
    <GLTFModel modelPath="/baseball_cap_ny.glb" />
  </group>
</BlankScene>

## Wizard Hat

For this hat, we've used cylinder.
The top of the cylinder covers inside of the hat.
The bottom of the cylinder acts like forehead and blocks parts behind it.

Besides this we've left some space between cylinder and facemesh.
Without the space the cylinder could sometimes bump into the facemesh.

[Download Occluder](/wizard_hat_occluder.glb)

<BlankScene key="hat">
  <group scale={[0.2, 0.2, 0.2]}>
    <GLTFModel modelPath="/canonical_face_model.glb" />
    <GLTFModel modelPath="/wizard_hat_occluder.glb" />
    <GLTFModel modelPath="/wizard_hat.glb" />
  </group>
</BlankScene>
